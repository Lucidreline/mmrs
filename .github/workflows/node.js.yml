name: Node.js CI

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

jobs:
  build:
    runs-on: self-hosted

    steps:
      - uses: actions/checkout@v2

      - run: npm i
      - run: npm i --prefix client
      - run: npm i --prefix server
      - run: npm run build
      - run: pm2 restart mmrs
